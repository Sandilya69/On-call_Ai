import type { ReactNode } from "react";

export default function DashboardLayoutShell({ children }: { children: ReactNode }) {
  // Placeholder shell. आगे PRD के हिसाब से sidebar/header yahan connect होंगे।
  return children;
}
