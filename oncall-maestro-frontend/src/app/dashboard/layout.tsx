import type { ReactNode } from "react";

export default function DashboardLayout({ children }: { children: ReactNode }) {

  // PRD के हिसाब से यहाँ Sidebar + Header + main wrapper आएगा।
  return children;
}
